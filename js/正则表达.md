- 正则表达
~~~
数组转json字符串
JSON.stringify(arr);

json字符串转json对象
data=eval('('+result.response+')');
JSON.pare(response)

邮箱校验
stremail.search(/^\w+((-\w+)|(\.\w+))*\@[A-Za-z0-9]+((\.|-)[A-Za-z0-9]+)*\.[A-Za-z0-9]+$/)>-1;

电话移动号码验证
/(\(\d{3,4}\)|\d{3,4}-|\s)?\d{7,14}/.test(num) true
~~~
